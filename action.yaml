apiVersion: automation.cloudbees.io/v1alpha1
kind: action
name: action-with-always

runs:
  using: composite
  steps:
    - name: action-should-run
      uses: docker://alpine:3.20
      if: ${{always()}}
      run: |
        echo "Running action"