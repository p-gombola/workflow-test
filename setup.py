from distutils.core import setup

setup(
    name='foo',
    version='0.1dev',
    packages=['foo',],
    license='No license',
    long_description='This is just a sample project to see if it builds',
)